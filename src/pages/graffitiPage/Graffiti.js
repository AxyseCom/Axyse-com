import React, { Component } from 'react'
import GraffitiSlider from "../graffitiPage/components/GraffitiSlider"

export default class Graffiti extends Component {
    render() {
        return (
            <GraffitiSlider/>
        )
    }
}